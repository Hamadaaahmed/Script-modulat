# Fixtures
Tests currently build minimal manifests in temporary directories to keep fixtures small and deterministic. Add durable malformed/edge-case fixtures here only when they improve readability or reproduce a regression.
